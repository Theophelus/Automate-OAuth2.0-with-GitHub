<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API AUTOMATION REPORT</title>
</head>
<style>
    body {
        font-family: 'Rubik Medium';
        margin: 0;
        padding: 0;
        background-color: #fff;
        color: Off White;
        font-weight: medium;
        font-style: medium;
    }

    .container {
        position: relative;
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
    }
    header {
       font-weight: bold;
       position: relative;
       display: flex;
       align-items: center;
       justify-content: space-between;
       margin-bottom: 10px;
    }

   header h1 {
       margin: 0;
       font-size: 20px;
   }

   header p {
       margin: 0;
       font-size: 14px;
   }
   header-line {
       border: none;
       border-top: 1px solid rgba(0, 0, 0, 0.1);
       box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
       color: #b2b09b;
       margin-bottom: 40px
   }
   .logo {
        width: 50px;
        height: 50px;
        background-color: #ff6f59; /* Background color */
        border-radius: 50%; /* Make it circular */
        display: flex;
        justify-content: center;
        align-items: center;
        font-size: 24px;
        font-weight: bold;
        color: White; /* Text color */
   }
    .stats {
      display: flex;
      justify-content: space-between;
      margin-bottom: 30px;
  }

  .stats div {
      flex-basis: 30%;
      padding: 20px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      text-align: center;
  }
  .stats div p {
      font-weight: bold;
  }

  .stats div:nth-child(1) {
      margin-left: 5px;
      background-color: #ff6f59; /* Off white */
      color: #333; /* Keep text color same as body */
  }

  .stats div:nth-child(2) {
      margin-left: 5px;
      background-color: #254441; /* Dark green */
      color: #fff;
  }

  .stats div:nth-child(3) {
      margin-left: 5px;
      background-color: #ff3c38; /* Change color for third stat */
      color: #333; /* Adjust text color for better visibility */
  }
</style>
<body>
<div class="container">
    <header>
        <div>
            <h1>API AUTOMATION REPORT</h1>
            <p> {{{currentDate}}} </p>
        </div>
        <div class="logo">AT</div>
    </header>
    <hr class="header-line">

    {{{statistics}}}

    {{{executionInfo}}}

    {{{tables}}}

</div>
</body>
</html>